version: '3.9'

services:

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tms_frontend
    ports:
      - "3003:3000"
    networks:
      - tms_net

networks:
  tms_net:
    external: true
